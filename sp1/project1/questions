Print errors to STDOUT or STDERR?
  stderr

How to handle bad commands, upper/mixed case commands, or invalid command parameters?
  assume all params are syntactically correct

Why a discrepency between `ls -lh` GNU `size` (17k vs 6010) ?
